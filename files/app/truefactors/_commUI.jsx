var _commUI = function () {
    this.videoBox = function() { 
            var _videoBox = React.createClass({
                    render : function () { 
                        return (<span>test</span>)
                    }
                });
            return <_videoBox>
        }
};
